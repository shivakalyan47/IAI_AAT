# ── Backend ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0            # async HTTP client for Gemini API calls
python-dotenv>=1.0.0     # loads .env files
pydantic>=2.7.0          # data validation (FastAPI dependency)

# ── Frontend ─────────────────────────────────────────────
streamlit>=1.35.0
requests>=2.31.0         # sync HTTP for Streamlit → backend calls

# ── Shared ───────────────────────────────────────────────
# pydantic is already listed above — used by both layers
